/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    reactRefresh: true,

    // eslint: {
    //   dirs: ["src", "pages", ""],
    // },
};

module.exports = nextConfig;
